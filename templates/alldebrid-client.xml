<?xml version="1.0" encoding="UTF-8"?>
<Container version="2">
  <Name>AllDebrid-Client</Name>
  <Repository>kroeberd/alldebrid-client:latest</Repository>
  <Registry>https://hub.docker.com/r/kroeberd/alldebrid-client</Registry>
  <Network>host</Network>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://discord.gg/8Vb9cj4ksv</Support>
  <Project>https://github.com/kroeberd/alldebrid-client</Project>
  <Icon>https://raw.githubusercontent.com/kroeberd/alldebrid-client/main/docs/logo.png</Icon>
  <Overview>
[b]AllDebrid-Client v1.8.23[/b] — Self-hosted torrent automation via AllDebrid.

Add magnet links or .torrent files through the Web UI, Jackett/Prowlarr search, a watch folder, Sonarr/Radarr (native qBittorrent API), or the REST API. AllDebrid-Client handles the full lifecycle: upload → poll → parallel unlock → aria2 download → notify.

[b]Features:[/b]
• Full lifecycle: upload → poll → unlock (parallel) → aria2 → done → Discord
• Built-in aria2 — default, no extra setup required (or use your own external aria2)
• Sonarr / Radarr native integration via qBittorrent v4.3.2 API emulation at /api/v2/
• Prowlarr support — modern Jackett alternative with native *arr integration
• Jackett multi-indexer search with chip picker and bulk Add / Add Selected / Add All
• Server-Sent Events (SSE) — instant live updates, no 15-second polling
• HTTP Basic Auth — optional access control (Settings → Access Control)
• Disk space guard — abort download before start if free space below threshold
• Post-processing script — shell command after each completed download
• Symlink / .url downloader — for rclone AllDebrid mount setups
• File selection — preview and block individual files before download
• Watch folder — drop .torrent or .magnet files for auto-import
• Auto-extraction — zip, rar, 7z, tar.* after download
• Discord rich embeds per event type (add / complete / error / no-peers / stats)
• Statistics: period selector 1h / 24h / 7d / 30d / 1y / all-time
• FlexGet v3 integration (schedule + manual trigger from the UI)
• Prometheus metrics at /api/metrics (Grafana-ready)
• SQLite (default, zero-config) or external PostgreSQL
• Automatic event log TTL, database backups, JSON export
• Token-bucket rate limiter for AllDebrid API
• Interactive API docs at /docs (Swagger UI)
• Built-in Help — Quick Start, Sonarr/Radarr setup, Troubleshooting

[b]Quick Setup:[/b]
1. Set paths for Config, Data, Watch, Processed, and Downloads
2. Open http://[IP]:8080 → Settings → General → enter AllDebrid API key → Save
3. Built-in aria2 starts automatically — no further setup required
4. (Optional) Add as qBittorrent download client in Sonarr/Radarr: Host=[IP], Port=8080
5. Add magnets via UI, search, or drop files in the Watch folder

[b]Sonarr / Radarr:[/b]
Settings → Download Clients → + → qBittorrent → Host: [IP], Port: 8080
No username/password needed unless you enable HTTP Basic Auth.

[b]Network:[/b] Uses host network mode to avoid conflicts with Unraid shares (shfs).
  </Overview>
  <Description>
    Self-hosted torrent automation via AllDebrid. Built-in aria2, qBittorrent API emulation for Sonarr/Radarr, Prowlarr/Jackett search, SSE live updates, HTTP Basic Auth, Prometheus metrics, Symlink downloader, file selection, Discord webhooks, FlexGet, SQLite/PostgreSQL.
  </Description>
  <Beta>False</Beta>
  <Category>Downloaders:</Category>
  <WebUI>http://[IP]:8080/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/kroeberd/unraid-templates/main/templates/alldebrid-client.xml</TemplateURL>
  <ExtraParams>--restart=unless-stopped</ExtraParams>
  <Date>2026-05-15</Date>

  <!-- ── Volumes ─────────────────────────────────────────────────────── -->

  <Config
    Name="Config Folder"
    Target="/app/config"
    Default="/mnt/user/appdata/alldebrid-client/config"
    Mode="rw"
    Description="[REQUIRED] Stores config.json. Must be persistent across restarts."
    Type="Path"
    Display="always"
    Required="true"
    Mask="false">/mnt/user/appdata/alldebrid-client/config</Config>

  <Config
    Name="Data Folder"
    Target="/app/data"
    Default="/mnt/user/appdata/alldebrid-client/data"
    Mode="rw"
    Description="[REQUIRED] Stores the SQLite database and automatic backups."
    Type="Path"
    Display="always"
    Required="true"
    Mask="false">/mnt/user/appdata/alldebrid-client/data</Config>

  <Config
    Name="Watch Folder"
    Target="/app/data/watch"
    Default="/mnt/user/data/torrents/watch"
    Mode="rw"
    Description="Drop .torrent or .magnet files here for automatic import. Leave empty if unused."
    Type="Path"
    Display="always"
    Required="false"
    Mask="false">/mnt/user/data/torrents/watch</Config>

  <Config
    Name="Processed Folder"
    Target="/app/data/processed"
    Default="/mnt/user/data/torrents/processed"
    Mode="rw"
    Description="Torrent/magnet files are moved here after processing. Leave empty if unused."
    Type="Path"
    Display="always"
    Required="false"
    Mask="false">/mnt/user/data/torrents/processed</Config>

  <Config
    Name="Download Folder"
    Target="/download"
    Default="/mnt/user/downloads/alldebrid"
    Mode="rw"
    Description="[REQUIRED] Downloaded files land here. If using external aria2, this path must match aria2's download directory."
    Type="Path"
    Display="always"
    Required="true"
    Mask="false">/mnt/user/downloads/alldebrid</Config>

  <!-- ── Environment Variables ───────────────────────────────────────── -->

  <Config
    Name="PUID"
    Target="PUID"
    Default="1000"
    Mode=""
    Description="User ID for downloaded files. Must match the UID of the user running Sonarr, Radarr, Plex, etc. Run 'id' in a terminal on the host to find the right value."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">99</Config>

  <Config
    Name="PGID"
    Target="PGID"
    Default="100"
    Mode=""
    Description="Group ID for downloaded files. Must match the GID used by your other media containers. Run 'id' in a terminal on the host."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">1000</Config>

  <Config
    Name="UMASK"
    Target="UMASK"
    Default="002"
    Mode=""
    Description="File permission mask. 002 = group-writable (recommended when sharing files with other containers). 022 = group-readable only."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">002</Config>

  <Config
    Name="Timezone"
    Target="TZ"
    Default="Europe/Berlin"
    Mode=""
    Description="Container timezone for logs and Discord timestamps. Examples: Europe/Berlin, UTC, America/New_York."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">Europe/Berlin</Config>

  <Config
    Name="Log Level"
    Target="LOG_LEVEL"
    Default="INFO"
    Mode=""
    Description="Logging verbosity: INFO for normal operation, DEBUG for troubleshooting."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">INFO</Config>

  <Config
    Name="Database Type"
    Target="DB_TYPE"
    Default=""
    Mode=""
    Description="Leave empty for SQLite (default, no setup required). Set to 'postgres' for an external PostgreSQL database — configure the connection in Settings → Database."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="Config Path"
    Target="CONFIG_PATH"
    Default="/app/config/config.json"
    Mode=""
    Description="Internal config file path. Do not change unless you have a specific reason."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">/app/config/config.json</Config>

  <Config
    Name="DB Path"
    Target="DB_PATH"
    Default="/app/data/alldebrid.db"
    Mode=""
    Description="Internal SQLite database path. Do not change unless you have a specific reason."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">/app/data/alldebrid.db</Config>

</Container>
