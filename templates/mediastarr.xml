<?xml version="1.0" encoding="UTF-8"?>
<Container version="2">
  <Name>Mediastarr</Name>
  <Repository>kroeberd/mediastarr:latest</Repository>
  <Registry>https://hub.docker.com/r/kroeberd/mediastarr</Registry>
  <Network>bridge</Network>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://discord.gg/8Vb9cj4ksv</Support>
  <Project>https://mediastarr.de</Project>
  <Icon>https://raw.githubusercontent.com/kroeberd/mediastarr/refs/heads/main/logos/icon_new.png</Icon>
  <Overview>
[b]Mediastarr v7.1.1[/b] — Automated missing-content and quality-upgrade search for Sonarr and Radarr.

[b]Features:[/b]
• Multi-instance: unlimited Sonarr + Radarr instances (HD, 4K, Anime, …)
• IMDb rating filter — skip low-rated content
• Target resolution filter — only upgrade if below your quality target
• Configurable cooldown (1–365 days), daily search limit, jitter
• SQLite search history with title, year, type, timestamps
• Discord webhook — 6 event types + rich embeds with poster and fanart
• German and English UI — switch without restart
• 4 themes: Dark / Light / OLED / System
• First-run browser wizard — no config file editing required
• Optional password protection + CSRF-hardened API
• Brute-force login protection (IP-based lockout)
• Console logger (MSLog) for developers

[b]Quick setup:[/b]
1. Set Data Directory (required for persistence)
2. Open http://[IP]:7979 — the setup wizard starts automatically
3. Add Sonarr and/or Radarr instances, test the connection, done

[b]Note:[/b] Independent project. Not affiliated with Huntarr or Sonarr/Radarr.
  </Overview>
  <Description>
    Automated media search for Sonarr and Radarr. Multi-instance, IMDb/resolution filters, Discord webhook with rich embeds, SQLite history, optional password, DE/EN, v7.1.1.
  </Description>
  <Beta>False</Beta>
  <Category>Downloaders: MediaApp:Other</Category>
  <WebUI>http://[IP]:[PORT:7979]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/kroeberd/unraid-templates/main/templates/mediastarr.xml</TemplateURL>
  <ExtraParams>--restart=unless-stopped</ExtraParams>

  <!-- ── Required ────────────────────────────────────────────── -->
  <Config
    Name="WebUI Port"
    Target="7979"
    Default="7979"
    Mode="tcp"
    Description="Port for the Mediastarr web interface. Must match the port in your browser URL."
    Type="Port"
    Display="always"
    Required="true"
    Mask="false">7979</Config>

  <Config
    Name="Data Directory"
    Target="/data"
    Default="/mnt/user/appdata/mediastarr"
    Mode="rw"
    Description="[REQUIRED] Persistent storage for config.json and the SQLite search-history database. Without this the setup wizard restarts on every container restart."
    Type="Path"
    Display="always"
    Required="true"
    Mask="false">/mnt/user/appdata/mediastarr</Config>

  <!-- ── System ───────────────────────────────────────────────── -->
  <Config
    Name="Timezone"
    Target="TZ"
    Default="Europe/Berlin"
    Mode="rw"
    Description="Container timezone — used for all timestamps in the UI, logs and Discord messages. Examples: Europe/Berlin, America/New_York, Asia/Tokyo, UTC. Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones"
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">Europe/Berlin</Config>

  <!-- ── Security ─────────────────────────────────────────────── -->
  <Config
    Name="Dashboard Password"
    Target="MEDIASTARR_PASSWORD"
    Default=""
    Mode="rw"
    Description="[OPTIONAL] If set, the dashboard and all API endpoints require login with this password. Browser write requests are CSRF-protected automatically. Recommended when accessible outside a trusted LAN. Leave empty for open access."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="true"/>

  <!-- ── Optional pre-fill (skips wizard) ─────────────────────── -->
  <Config
    Name="Sonarr URL (pre-fill)"
    Target="SONARR_URL"
    Default=""
    Mode="rw"
    Description="[OPTIONAL] Pre-configure Sonarr URL to skip the setup wizard for this instance. Use the IP/hostname reachable from within this container, e.g. http://192.168.1.100:8989. Must also set SONARR_API_KEY."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false"/>

  <Config
    Name="Sonarr API Key (pre-fill)"
    Target="SONARR_API_KEY"
    Default=""
    Mode="rw"
    Description="[OPTIONAL] Pre-configure Sonarr API key. Found in Sonarr → Settings → General → Security → API Key. Only used when SONARR_URL is also set."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="true"/>

  <Config
    Name="Radarr URL (pre-fill)"
    Target="RADARR_URL"
    Default=""
    Mode="rw"
    Description="[OPTIONAL] Pre-configure Radarr URL to skip the setup wizard for this instance. Use the IP/hostname reachable from within this container, e.g. http://192.168.1.100:7878. Must also set RADARR_API_KEY."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false"/>

  <Config
    Name="Radarr API Key (pre-fill)"
    Target="RADARR_API_KEY"
    Default=""
    Mode="rw"
    Description="[OPTIONAL] Pre-configure Radarr API key. Found in Radarr → Settings → General → Security → API Key. Only used when RADARR_URL is also set."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="true"/>

</Container>
